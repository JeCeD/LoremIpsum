<%= form_for(@status) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="input-field col s8">
      <%= f.label :nombre, for:"nombre" %><br>
      <%= f.text_field :nombre, id:"nombre", class:"validate", required:"true" %>
    </div>
    <div class="input-field col s8">
      <%= f.label :contenido, for:"contenido" %><br>
      <%= f.text_area :contenido, id:"contenido", class:"materialize-textarea", required:"true" %>
    </div>
    <div class="actions col s12">
      <%= button_tag(type: 'submit', class: "btn") do %>
       Ok<i class="material-icons right">►</i>
      <% end %>
    </div>
  </div>
<% end %>
